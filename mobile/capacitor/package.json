{
  "name": "capacitor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync": "cap sync android",
    "sync:www": "cd ../../apps/web && npm run build && cd ../../mobile/capacitor && rm -rf www && cp -r ../../apps/web/client/dist www && find www \\( -name '*.gz' -o -name '*.br' \\) -delete && cap sync android"
  },
  "keywords": [],
  "type": "module",
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
